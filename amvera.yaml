meta:
  environment: node
  toolchain:
    name: npm
    version: 20

build:
  additionalCommands: npm install -g pnpm && pnpm install --frozen-lockfile && pnpm build

run:
  command: node_modules/.bin/next start
  containerPort: 3000
  persistenceMount: /data
