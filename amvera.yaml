meta:
  environment: node
  toolchain:
    name: npm
    version: 20

build:
  additionalCommands: npm install -g pnpm && pnpm install --frozen-lockfile && pnpm build

run:
  command: pnpm start
  containerPort: 3000
  persistenceMount: /data
